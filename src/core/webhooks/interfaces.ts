/**
 * Webhook Domain Interfaces
 *
 * Consolidates the webhook service interface so other parts of the application
 * have a single import path. Additional interfaces can be added here as the
 * domain grows.
 */

export { IWebhookService } from '@/core/webhooks/IWebhookService';
